{
  "name": "enrolled-hello",
  "version": "0.0.1",
  "private": true,
  "description": "The one lambda that IS registered in lambda-pr-check.yaml's detect filter + check matrix — the baseline happy-path case.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.15",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.4",
    "typescript": "^5.5.4"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
