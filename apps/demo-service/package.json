{
  "name": "demo-service",
  "version": "0.0.1",
  "private": true,
  "description": "Stand-in for a normal marketplace app — used to verify that PRs touching only this (not aws/lambdas/**) are NOT blocked by the path-filtered lambda-pr-check required check, thanks to the no-op pairing.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.15",
    "@nestjs/core": "^10.4.15",
    "@nestjs/platform-express": "^10.4.15",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.15",
    "typescript": "^5.5.4"
  }
}
